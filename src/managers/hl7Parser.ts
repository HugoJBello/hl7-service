import { SchemeField } from "../models/Schema";

const separator = "|"

export const parseUsingSchema = (hl7Item: string, schema: SchemeField[]): Object => {
  const result = {}


  for (const field of schema) {

  }
  return result
}